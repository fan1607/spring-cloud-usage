spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fan1607/spring-cloud-usage/
          search-paths: config-server
          username: yuefanlee@gmail.com
          password:

server:
  port: 7001
